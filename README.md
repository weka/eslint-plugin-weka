# eslint-plugin-weka
Internal javascript eslint plugin with single rule to use constant `EMPTY_STRING` instead of `''` or `""`.


## Publish new version

You have to create a new release, and on each release publishing of new version will happen automatically.
